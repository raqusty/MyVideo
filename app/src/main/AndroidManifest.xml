<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.video.raqust.myvideo">

    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 设置OpenGl的版本 OpenGl ES 2.0   -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".firstSection.FirstSectionActivity" />

        <activity android:name=".secondSection.SecondSectionActivity" />

        <activity android:name=".thirdSection.ThirdSectionActivity" />
        <activity android:name=".thirdSection.Nv21VideoActivity" />

        <activity android:name=".fourthSection.FourthSectionActivity" />
        <activity android:name=".fourthSection.MediaRecorderActivity" />
        <activity android:name=".fourthSection.ShootingActivity" />
        <activity android:name=".fourthSection.MainActivity" />

        <activity android:name=".fifthSection.FifthSectionActivity" />
    </application>

</manifest>